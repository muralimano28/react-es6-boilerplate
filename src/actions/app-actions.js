"use strict";

// This file will create all the actions for this app and will dispatch it to dispatcher.

const AppActions = {
    getData: (type, data) => {
        switch (type) {
            default:
                // Do nothing.
        }
    },
    sendData: (type, data) => {
        switch (type) {
            default:
                // Do nothing.
        }
    }
};

export default AppActions;
